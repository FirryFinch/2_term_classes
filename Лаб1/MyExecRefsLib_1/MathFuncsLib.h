namespace MathFuncs
{
    class MyMathFuncs
    {
		public:
        static double Add(double a, double b);			// возвращает a + b
        static double Subtract(double a, double b);		// возвращает a - b
		static double Multiply(double a, double b);		// возвращает a * b
		static double Divide(double a, double b);		// возвращает a / b
    };
}